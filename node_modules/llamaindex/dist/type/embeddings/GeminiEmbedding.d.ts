import { GEMINI_MODEL, type GeminiConfig, type GeminiSession } from "../llm/gemini.js";
import { BaseEmbedding } from "./types.js";
/**
 * GeminiEmbedding is an alias for Gemini that implements the BaseEmbedding interface.
 */
export declare class GeminiEmbedding extends BaseEmbedding {
    model: GEMINI_MODEL;
    temperature: number;
    topP: number;
    maxTokens?: number;
    session: GeminiSession;
    constructor(init?: GeminiConfig);
    private getEmbedding;
    getTextEmbedding(text: string): Promise<number[]>;
    getQueryEmbedding(query: string): Promise<number[]>;
}
